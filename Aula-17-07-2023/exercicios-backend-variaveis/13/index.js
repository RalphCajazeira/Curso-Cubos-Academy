// Input

const raioBase = 3
const alturaCilindro = 1
const PI = "PI"; //Só quis colocar aqui para juntar na Impressão da tela

let areaTotalCilindro;

// Calculos

areaTotalCilindro = 2 * raioBase * (raioBase + alturaCilindro);

// Imprimir na Tela

console.log(`A área total do cilindro é ≅ ${areaTotalCilindro + PI} `);